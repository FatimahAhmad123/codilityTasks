#ifndef DISTINCT.H
#define DISTINCT .H
#include <iostream>
#include <vector>
#include <algorithm>

namespace CodilityTasks
{
	class Distinct

	{
	public:
		int solution(std::vector<int> &A);
	};
}
#endif DISTINCT.H